server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  location /fhir/ {
    proxy_pass http://server:8080/fhir/;
  }
  location /auth/ {
    proxy_pass http://server:8080/auth/;
  }
  location / {
    try_files $uri /index.html;
  }
}
